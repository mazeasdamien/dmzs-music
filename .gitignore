# dependencies
node_modules/
downloader/.venv/

# secrets, these must NEVER end up in a repository
.dev.vars
.dev.vars.*
.env
downloader/.env
cookies.txt

# local configuration: carries your own database id and domain. Copy it from
# wrangler.example.jsonc, and keep that example current when this changes.
wrangler.jsonc

# build and runtime artefacts
.wrangler/
__pycache__/
*.pyc
*.log
.DS_Store
Thumbs.db
